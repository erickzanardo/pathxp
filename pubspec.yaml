name: pathxp
description: Like regular expressions but for defining paths in a grid
version: 0.4.0
repository: https://github.com/erickzanardo/pathxp

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  mocktail: ^1.0.0
  test: ^1.19.2
  very_good_analysis: ^5.1.0
